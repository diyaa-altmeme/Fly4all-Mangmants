# 🚀 دليل النشر (Deployment Guide)

هذا الدليل يوضح خطوات نشر تطبيق Mudarib Accounting على Firebase.

## المتطلبات الأساسية

1.  **حساب Google** و **مشروع Firebase**.
2.  **Node.js** (الإصدار 18 أو أحدث) و **npm** مثبتان على جهازك.
3.  تثبيت واجهة سطر الأوامر لـ Firebase عالميًا:
    ```bash
    npm install -g firebase-tools
    ```

## خطوات النشر

### الخطوة 1: تسجيل الدخول إلى Firebase

إذا لم تكن قد سجلت الدخول بالفعل، قم بتسجيل الدخول إلى حسابك في Google عبر الـ terminal:

```bash
firebase login
```

### الخطوة 2: ربط المشروع المحلي بمشروع Firebase

تأكد من أنك في المجلد الرئيسي للمشروع، ثم قم بربطه بمشروعك على Firebase:

```bash
firebase use [YOUR_FIREBASE_PROJECT_ID]
```

استبدل `[YOUR_FIREBASE_PROJECT_ID]` بمعرف مشروعك الفعلي على Firebase.

### الخطوة 3: التحقق من إعدادات البناء (Build)

تأكد من أن ملف `package.json` يحتوي على الـ `build script` الصحيح. يجب أن يكون كالتالي:

```json
"scripts": {
  "build": "next build --no-lint"
}
```

### الخطوة 4: بناء المشروع للإنتاج

قم بتشغيل أمر البناء لإنشاء نسخة محسّنة من تطبيقك جاهزة للنشر:

```bash
npm run build
```

**ملاحظة هامة:** تأكد من أن عملية البناء تكتمل بنجاح بدون أخطاء. إذا واجهت أي مشاكل، قم بحلها قبل المتابعة.

### الخطوة 5: نشر التطبيق

بعد اكتمال عملية البناء بنجاح، يمكنك الآن نشر تطبيقك على Firebase Hosting باستخدام الأمر التالي:

```bash
firebase deploy
```

سيقوم هذا الأمر برفع ملفات البناء إلى Firebase Hosting وإعداد الواجهة الخلفية (Backend) على App Hosting.

### الخطوة 6 (اختياري): النشر لقناة معاينة (Preview Channel)

إذا كنت ترغب في اختبار تغييراتك على رابط مؤقت قبل نشرها على الموقع الرسمي، يمكنك استخدام قنوات المعاينة:

```bash
firebase hosting:channel:deploy [CHANNEL_ID]
```

استبدل `[CHANNEL_ID]` باسم فريد لقناتك (مثال: `testing-new-feature`).

---

🎉 **تهانينا!** تم نشر تطبيقك بنجاح. يمكنك الآن زيارة رابط الاستضافة الخاص بك لعرض التطبيق.
