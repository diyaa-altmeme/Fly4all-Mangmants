rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ✅ قواعد مفتوحة تماماً للتأكد من أن المشكلة ليست في القواعد
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
