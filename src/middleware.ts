// This middleware is completely disabled to prevent conflicts with the app's routing and authentication logic.
// The necessary redirection and protection logic is now handled within the MainLayout component.
export const config = {
  matcher: [],
};
